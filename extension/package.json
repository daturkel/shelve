{
  "name": "@shelve/extension",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/manifest.json",
    "watch": "vite build --watch",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "screenshot": "node scripts/generate-readme-screenshot.mjs"
  },
  "dependencies": {
    "@shelve/core": "*",
    "@shelve/shared": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/chrome": "^0.0.324",
    "happy-dom": "^20.10.6",
    "playwright": "^1.61.1",
    "typescript": "^5.9.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
