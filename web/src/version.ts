// Hand-kept in sync with this package's package.json "version" — mirrors
// worker/src/version.ts's WORKER_VERSION, since the web app doesn't read
// its own package.json at build time either.
export const WEB_VERSION = "0.4.0";
